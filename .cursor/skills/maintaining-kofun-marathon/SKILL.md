---
name: maintaining-kofun-marathon
description: >
  古墳マラソンのコース作成・表示・公開を、合意済みの使い方に合わせて直すスキル。
  「古墳マラソンを直して」「推奨古墳を変えて」「走行距離を出して」「特になしと表示して」
  「案内をグループ化して」「スマホだけで使えるようにして」と依頼された際に使用する。
---

# 古墳マラソンを直す

地名と走りたい距離から古墳を勧め、選んだ古墳だけの周回と走行距離を出す。説明は押しやすく、ない情報は「特になし」にする。

## Outcome

作業が終わったとき、次が揃っている。

- 地名と希望距離のあとに、推奨古墳が出る
- 推奨古墳の説明は、押さなくても各古墳の下に出る。記事や記載がなければ「特になし」
- 推奨のうち選んだ古墳だけで道順が組み直され、同じ画面に走行距離が出る
- 案内、各地の古墳、走行距離、操作ボタンは、余白のある別々のまとまりになっている
- スマホだけで使う依頼なら、公開ページが更新され、読み込み直しでその状態を開ける

## 依存

- 画面とコース計算は `src/` の実装が原本。このスキルは画面の振る舞いだけを定める
- スマホ向けの公開は `.github/workflows/pages.yml` の公開手順に従う。公開先のアドレスはリポジトリの Pages 設定を見る

実装の関数名・画面の部品名・見た目の数値は、作業時にコードを読んで合わせる。このスキルへ写さない。

## 使う人の流れ

1. 地名を入れ、走りたい距離を入力する。
2. その距離に近い推奨古墳を表示する。近くの古墳を全部並べて選ばせない。
3. 推奨古墳の説明は、押さなくても各古墳の下に出る。
4. 推奨古墳のうち、回りたいものだけを選ぶ。3基出て2基だけ選んだら、その2基で道順を組み直す。
5. 選んだ古墳を最短で回り、起点に戻る道のりを走行距離として、選択と同じ画面に出す。

## 表示

- 操作案内は1文ずつ分け、あいだに余白を置く。案内、各地の古墳、走行距離、操作ボタンは別のまとまりにする。
- ウィキペディアに記事がない、または年代・サイズ・特記事項の記載がないときは、その欄を「特になし」にする。「見つかりませんでした」とは出さない。
- 走行距離は、チェックを変えたあとの道のりの長さにする。

## スマホだけで開く

利用者がスマホだけで使うときは、パソコンを起動したままにする仮の通り道では済ませない。変更を公開手順で載せ、携帯回線だけで開ける状態にする。公開後は、利用者がページを読み込み直す必要があることを伝える。

## 直したあとの確認

Outcome の各項目を、画面で満たしているか確認する。満たすまで完了にしない。
